# VoicePrint — AI Text Humanizer
# Core deps only. Heavy ML packages (torch, transformers, etc.) are lazy-loaded
# at runtime and only needed for model-based detection. They can be installed
# separately via: pip install voiceprint[full]

# Frontend
streamlit>=1.35

# LLM API client (unified interface for OpenAI, Gemini, Groq, etc.)
litellm>=1.40

# Readability metrics
textstat>=0.7

# Math — used eagerly in metrics.py and patterns.py
numpy>=1.24

# Testing
pytest>=7.0
